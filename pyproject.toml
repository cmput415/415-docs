[project]
name = "415-docs"
version = "0.0.0"
description = "CMPUT 415 course specification documents (Sphinx sources)."
requires-python = ">=3.10"
dependencies = [
    "sphinx==6.2.1",
    "sphinx-rtd-theme==1.2.0",
    "jinja2>=3.1",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
